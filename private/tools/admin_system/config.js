class CONFIG {
    constructor(parent) {
        this.parent = parent;
        this.initialize();
    }
    /** Inicjalizacja modułu CONFIG */
    initialize() {
        console.log('Inicjalizacja CONFIG...');
        // Tutaj możesz dodać kod inicjalizacyjny, np. rejestrację w menu startowym
        // lub innych elementów interfejsu użytkownika.
    }
}

export default CONFIG;